# @ahamie/cli

The `ahamie` binary. Verbs:

```
ahamie create | dev | build | deploy | ui add | publish | install | login | logout | run | eval | triggers | db migrate | db studio | secrets | factory run | doctor
```

Plugins extend the surface via `package.json` `ahamie.plugin` entries (v1).
